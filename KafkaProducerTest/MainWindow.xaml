<Window x:Class="KafkaProducerTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:KafkaProducerTest"
        Title="Kafka Producer Test" Height="550" Width="900">
    
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>

        <!-- Left: Controls and sent messages -->
        <StackPanel Grid.Column="0">
            
            <!-- Single: JSON editor -->
            <GroupBox Header="Сообщение (одиночное)" Margin="0,0,0,5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Button Content="Генерировать JSON" Command="{Binding GenerateSampleCommand}" Grid.Row="0" HorizontalAlignment="Left" Margin="4" />

                    <TextBox Grid.Row="1" 
                             Text="{Binding SelectedMessageJson, UpdateSourceTrigger=PropertyChanged}" 
                             AcceptsReturn="True" 
                             FontFamily="Consolas" 
                             FontSize="12" 
                             VerticalScrollBarVisibility="Visible" 
                             HorizontalScrollBarVisibility="Auto" 
                             TextWrapping="NoWrap" />

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="4" HorizontalAlignment="Left">
                        <Button Content="Отправить" Command="{Binding SendOneCommand}" Width="100" Margin="0,0,8,0" />
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- Series controls -->
            <GroupBox Header="Серия сообщений" Margin="0,0,0,5">
                <StackPanel Margin="4">
                    
                    <StackPanel Orientation="Horizontal" Margin="0,4">
                        <TextBlock Text="Количество (-1 для постоянной отправки):" VerticalAlignment="Center" Width="250" />
                        <TextBox Text="{Binding Count}" Width="80" />
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,4">
                        <TextBlock Text="Интервал (мс):" VerticalAlignment="Center" Width="250" />
                        <TextBox Text="{Binding IntervalMs}" Width="80" />
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,8">
                        <Button Content="Старт" Command="{Binding StartSeriesCommand}" Width="100" Margin="0,0,8,0" />
                        <Button Content="Стоп" Command="{Binding StopSeriesCommand}" Width="100" />
                    </StackPanel>
                    
                </StackPanel>
            </GroupBox>


            <GroupBox Header="Инфо" Margin="0,0,0,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.ColumnSpan="2" Text="Настройки Kafka (переменные окружения):" FontWeight="Bold" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="BootstrapServers:" HorizontalAlignment="Right" Margin="5,0" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding KafkaBootstrapServers}" Margin="5,0" />
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="KafkaTopic:" HorizontalAlignment="Right" Margin="5,0" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding KafkaTopic}" Margin="5,0" />
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="KafkaGroupId:" HorizontalAlignment="Right" Margin="5,0" />
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding KafkaGroupId}" Margin="5,0" />

                    <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Text="Default:  bootstrap: localhost:9092, topic: test-topic, groupId: kafka-producer-test" Margin="0,4,0,0" TextWrapping="Wrap" />

                </Grid>
            </GroupBox>

            <GroupBox Header="Внимание" >
                <TextBlock TextWrapping="Wrap" Margin="6">
                    Сообщения отправляются в формате JSON:
                    userId: int,
                    eventType: string,
                    timestamp:ISO8601,
                    data.buttonId:string.
                </TextBlock>
            </GroupBox>
        </StackPanel>

        <StackPanel Grid.Column="1">

            <GroupBox Header="Отправленные сообщения (сначала новые)" Margin="0,0,0,0" Height="500">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding SentMessages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" Margin="4" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </GroupBox>
        </StackPanel>
    </Grid>
</Window>